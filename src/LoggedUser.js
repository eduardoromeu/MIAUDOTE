export default class LoggedUser {
    constructor(logged, name, password, email, avatar, favorites) {
        this.logged = logged;
        this.name = name;
        this.password = password;
        this.email = email;
        this.avatar = avatar;
        this.favorites = favorites;
    }
}	